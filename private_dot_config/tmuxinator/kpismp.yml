name: kpismp
root: ~/projects/kpismp

pre_window: cd frontend
startup_window: editor

windows:
  - build: npm run start -- --port 4200
  - editor: nvm use default; nvim
  - test: nvm use 16; npx ng test @emias-clinician/smp --watch --code-coverage --browsers Chrome
