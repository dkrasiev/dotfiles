name: vop
root: ~/Documents/emias/kpiprofilevop
  
pre_window: cd frontend && nvm use 14
  
startup_window: editor

windows:
  - build:
      layout: even-horizontal
      panes:
        lib: npm run lib:watch
        app: npm start -- --port 4300
  - editor: nvim
